大小写：
    Redis 命令关键字（如 SET, GET）不区分大小写，可以随意使用大写、小写或混合。
    键（Key）和值（Value）默认区分大小写，需保持一致。

Redis不和MySQL一样用;结束语句

Redis数据库是使用键值对的形式存储数据，类似python的字典形式

Redis的字符串可以不写引号也可以使用单引号和双引号
注意事项：没有引号：如果有空格的情况下会让程序认为是单独的字符